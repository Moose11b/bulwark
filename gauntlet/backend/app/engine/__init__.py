"""Exercise engines: the MSEL branching logic, adjudication, and reporting.

These modules are pure functions over plain dicts and ORM objects — no I/O — so
they are trivial to unit-test and could later run inside a range or a worker
without change.
"""
