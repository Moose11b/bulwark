"""Authoring (M2): a threat-actor & inject catalog, a scenario generator that
grounds a branching MSEL in a real environment, and multi-channel delivery.

Generation is deliberately deterministic and self-contained — no external model
call — so a generated scenario is reproducible and reviewable. It is a starting
draft: the proctor edits it before running.
"""
